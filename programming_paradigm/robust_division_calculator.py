def safe_divide(numerator, denominator):
    if denominator == 0:
        raise ZeroDivisionError ("Division by zero is not allowed")
    return safe_divide()

try:
    if denominator == zero:
        raise ValueError ("Input numeric values")
    