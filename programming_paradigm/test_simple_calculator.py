import unittest
from simple_calculator import SimpleCalculator

class TestSimpleCalculator(unittest.TestCase):
    def test_calculator(self, add, subtract, multiply, divide)
    self.calculator = SimpleCalculator()
    def test_addition(self):
        self.assertEqual(self.calculator.add(x, y))
        result = x + y
        return result
    def test_subtraction(self):
        self.assertEqual(self.calculator.subtract(x, y))
        result = x - y
        return result
    def test_multiplication(self):
        self.assertEqual(self.calculator.multiply(x, y))
        result = x * y
        return result
    def test_division(self):
        self.assertEqual(self.calculator.divide(x, y))
        result = x / y
        return result

